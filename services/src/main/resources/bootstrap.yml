# Application config

server:
 port: 9081
 contextPath: /services 

spring:
 application:
  name: i3-services
 profiles:
  active: dev 
spring.cloud.config.uri: http://localhost:11001

# Discovery Server Access
eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:11000/eureka
 